import { ActivefilterPipe } from './activefilter.pipe';

describe('ActivefilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ActivefilterPipe();
    expect(pipe).toBeTruthy();
  });
});
